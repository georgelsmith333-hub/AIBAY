## Packages
framer-motion | Smooth animations for page transitions and card interactions
lucide-react | Iconography
clsx | Class name utility (already likely installed but good to ensure)
tailwind-merge | Class merging (already likely installed)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
